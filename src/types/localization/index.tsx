import { List } from '@/types/localization/list'
import { Locale } from '@/types/localization/locale'
import { Translation } from '@/types/localization/translation'
import { I18nextResources } from '@/types/localization/i18next-resources'

/**
 * Exported modules:
 * - type `Translations` List of translations for the locale
 * - type `Locale` Locale for storing metadata if single locale
 * - type `List` List for storing all locales and their metadata
 *
 * @exports {Object} - An object containing the exported modules.
 */
export type { List, Locale, Translation, I18nextResources }
