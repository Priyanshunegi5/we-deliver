import { MultiFactor } from '@/enums/multifactor'

/**
 * Exported modules:
 * - enum `MultiFactor` List of supported authentication options.
 *
 * @exports {Object} - An object containing the exported modules.
 */
export { MultiFactor }
